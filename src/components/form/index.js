import React from "react";

const Form = () => (
  <form className="container-welcome__form">
    <input
      type="search"
      className="container-welcome__form--input"
      placeholder="Search for another Artist"
    ></input>
  </form>
);

export default Form;
